db:
  name: ${DJANGO_DB_NAME}
  user: ${DJANGO_DB_USER}
  password: ${DJANGO_DB_PASSWORD}
  host: localhost
  port: 5433

secret.key: ${DJANGO_SECRET_KEY}

rabbitmq:
  host: localhost

---
spring.activate.on-profile: docker

db:
  host: postgresql-2
  port: 5432

rabbitmq:
  host: rabbitmq

